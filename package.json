{
  "name": "root",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd dashboard && bun run dev",
    "build": "cd dashboard && bun install && bun run build",
    "build:dev": "cd dashboard && bun install && bun run build:dev",
    "preview": "cd dashboard && bun run preview",
    "lint": "cd dashboard && bun run lint"
  }
}
